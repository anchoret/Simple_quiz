[common]
    routing = %kernel.root_dir%/config/routing.ini
    services = %kernel.root_dir%/config/service.ini
    cache.folder = %kernel.root_dir%/cache
[database]
    driver   = mysql
    host     = localhost
    port     = 3306
    name     = quiz
    user     = quiz
    password = quiz
    charset  = UTF8